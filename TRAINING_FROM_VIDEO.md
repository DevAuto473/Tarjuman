# التدريب من الفيديو — الدليل الكامل

> الطريقة الجديدة: تُسجَّل الإشارات فيديوهاتٍ مسبقاً، ثمّ تُستخرج السمات
> منها دفعةً واحدة. بديلٌ عن `npm run collect` الذي يلتقط من الكاميرا حيّاً.

---

## الأمر الواحد

```
npm run trainstill
```

يفعل الخطوتين معاً: يستخرج السمات من كلّ فيديو في `data/DATA/`، ثمّ يدرّب
النموذج على الناتج.

---

## لماذا الفيديو أفضل من الالتقاط الحيّ

الالتقاط الحيّ مقيَّدٌ بمعدّل الإطارات: كلّ مللي ثانية تُنفقها على الدقّة
تُقتطع من سلاسة العرض. فاضطُرّ إلى تنازلات. والفيديو المسجَّل لا سقف زمني
له، فتزول التنازلات كلّها:

| | `npm run collect` (حيّ) | `npm run trainstill` (فيديو) |
|---|---|---|
| نموذج اليدين | `complexity=0` — الأسرع | **`complexity=1`** — الأدقّ |
| نموذج الجسم | مرّة كلّ **٥** إطارات | **كلّ** إطار |
| اليد تسقط من الرصد | تدخل أصفاراً في منتصف الحركة | **تُستوفى** رياضياً |
| انقلاب اليمين/اليسار | يمرّ ويُفسد المسار | **يُصحَّح** |
| اللقطة الرديئة | تدخل المجموعة | **تُرفض** ويُطبع سببها |

الفارق الحقيقي ليس الدقّة وحدها، بل **المرّة الثانية**: حين تقرأ الفيديو
كاملاً تستطيع أن تنظر إلى ما بعد الإطار الحالي. وهذا مستحيلٌ حيّاً — لا
تعرف المستقبل وأنت تلتقط.

---

## بنية المجلّدات

مجلّدٌ لكلّ كلمة، واسمه هو مفتاح الربط:

```
data/DATA/
    ahln/        ahln_001.mp4  ahln_002.mp4  …
    shkrn/       shkrn_001.mp4  …
    9ba7_al5er/  …
```

الامتدادات المقبولة: `.mp4` `.mov` `.avi` `.mkv` `.webm`
المجلّد الفارغ يُتجاهَل بلا شكوى.

### ما يُتوقَّع من كلّ فيديو

- **إشارةٌ واحدة**، مقصوصةٌ على الحركة: يبدأ معها وينتهي معها.
- اليدان داخل الإطار، **والكتفان مرئيّان**. المراسي الخمس تُقاس من الجسم
  لا من اليد — أنف وفم وأذن وكتف وصدر — وهي ما يفصل *أب* عن *أم*
  و*أخ* عن *أخت*: شكل اليد وحركتها متطابقان، والفارق أين تقف.
- إضاءةٌ كافية. ضبابية الحركة أوّل ما يُسقط الرصد.

المعدّل والدقّة والمدّة حرّة: يُقرأ المعدّل من الملفّ نفسه، ويُعاد التشكيل
إلى ٣٠ إطاراً، وتُطبَّع المُجمَلات على ٢٠ إطاراً/ث.

---

## الخطوات الثلاث

### ١. راجع ملفّ الربط — لا تتخطَّ هذه

`data/DATA/labels_map.json` يربط اسم المجلّد بمعرّف الكلمة في
`vocabulary.py`.

**لماذا يلزم:** مجلّداتك معرَّبة (`ahln`) ومعرّفات النظام إنجليزية
(`hello`). بلا ربطٍ تدخل الكلمةُ الواحدة **تسميتين**، فيتعلّم المصنّف
أنّهما شيئان مختلفان — ثمّ يتردّد بينهما في كلّ استدلال.

الملفّ ثلاثة أقسام:

| القسم | ماذا تفعل |
|---|---|
| **مؤكَّدة** | تطابق القاموس نصّاً — اتركها |
| **مرجَّحة** | قريبةٌ لا مطابقة — راجعها بعينك |
| **جديدة** | ليست في القاموس — اتركها لتدخل كتسمية جديدة، أو بدّلها بمعرّف موجود، أو **احذف السطر** لتُستثنى الكلمة كلّها |

المفاتيح التي تبدأ بشرطةٍ سفلية (`_مؤكَّدة`) تعليقاتٌ تُتجاهَل.

### ٢. جرّب قبل أن تكتب

```
npm run trainstill -- --dry-run
```

يستخرج ويفحص ويطبع التقرير، ولا يكتب ولا يدرّب. **اقرأ المخرجات قبل
المتابعة.**

### ٣. شغّل

```
npm run trainstill
```

---

## ⚠️ مصيدة العكس المزدوج — اقرأ هذه

المسار الحيّ يعكس صورة الكاميرا (`MIRROR_FRAME = True`)، والمُعالج يفعل
مثله افتراضاً حتى يتطابق الفضاءان.

**لكنّ تسجيل الكاميرا الأمامية في الهاتف قد يُحفَظ معكوساً أصلاً.** وحينها
يكون العكس مرّتين، فتنقلب اليمنى واليسرى في المجموعة كلّها — **بصمت**، بلا
خطأ ولا تحذير. النموذج يتدرّب على مرآةٍ لما يراه في الاستدلال، فتنهار
الدقّة بلا سببٍ ظاهر.

**الفحص:** في مخرجات `--dry-run` انظر سطر النسب:

```
   hello          ahln_001.mp4
        52 إطار @ 30  (1.73 ث)   يسرى   4%  يمنى  96%
```

إن أدّيتَ الإشارة **بيمناك** وظهرت «يسرى ٩٦٪»، فالفيديو معكوس. أعد
التشغيل مع:

```
npm run trainstill -- --no-mirror
```

افحص هذا مرّةً واحدة على أوّل فيديو. إن كانت كلّ التسجيلات بنفس الكاميرا
ونفس التطبيق، فالحكم واحدٌ لها جميعاً.

---

## ماذا يحدث لكلّ فيديو

```
قراءة الإطارات ومعدّلها من الملفّ
          ↓
Hands (c=1)  +  Pose (c=1، كلّ إطار)      ← رصدٌ أدقّ ممّا يُحتمل حيّاً
          ↓
تثبيت اليمين واليسار عبر الفيديو كاملاً    ← بالتقارب المكاني
          ↓
سدّ الفجوات الداخلية بالاستيفاء الخطّي     ← سقف ربع ثانية
          ↓
بناء متّجه ١٤٤ قيمة لكلّ إطار              ← بدوالّ المسار الحيّ نفسها
          ↓
حساب المُجمَلات الـ١٢ (مدّة، إيقاع، اتّجاه) ← قبل إعادة التشكيل
          ↓
إعادة التشكيل إلى ٣٠ إطاراً
          ↓
بوّابة الجودة → صفٌّ في dynamic_gestures_v6.csv
```

### تخطيط المتّجه

```
٦٨ لكلّ يد  =   ٣ المعصم بإحداثيات الجسم
             + ٦٠ عشرون معلماً نسبةً إلى المعصم، مُطبَّعة على طول الكفّ
             +  ٥ مسافات إلى المراسي

١٤٤ للإطار =  ٦٨ × ٢  +  ٨ كتلة الجسم
الصفّ      =  label + وصف + ٣٠×١٤٤ + ١٢ مُجمَلاً  =  ٤٣٣٨ عموداً
```

### ثلاثة قيود في الاستيفاء، مقصودة

- **الفجوات الداخلية فقط.** غياب اليد في أوّل الفيديو أو آخره ليس نقصاً
  يُملأ بل هو ما حدث. وفي إشارةٍ بيدٍ واحدة تغيب الأخرى طوال الوقت، وتلك
  **معلومة** (`hands_used`) لا ثغرة.
- **سقف ربع ثانية.** ما جاوزه اختراعُ حركةٍ لا تقديرها. يُترك ويُبلَّغ عنه.
- **خطّيٌّ لا تكعيبي.** التكعيبي يتجاوز القيم عند الحواف فيخترع تسارعاً لم
  يقع — و`peak_speed` من المُجمَلات تُقرأ من هذه البيانات بالذات.

والاستيفاء يقع **على المعالم الخام قبل الاستخراج**، لا على المتّجه. المتّجه
فيه كمّياتٌ مشتقّة — شكلٌ مُطبَّع، ومسافاتُ مراسٍ، وراية رصد — واستيفاؤها
مباشرةً يُخرج كفّاً مستحيلاً هندسياً ورايةَ رصدٍ بقيمة ٠٫٦.

---

## قراءة التقرير

```
   hello          ahln_001.mp4
        52 إطار @ 30  (1.73 ث)   يسرى   4%  يمنى  96%
        مُلئت 3 إطاراً بالاستيفاء
        صُحّح 2 انقلاباً في نسبة اليدين
        -> مقبول
```

| السطر | ماذا يعني | متى تقلق |
|---|---|---|
| `يسرى/يمنى %` | نسبة الإطارات التي رُصدت فيها كلّ يد | أقلّ من ٥٠٪ للِيد العاملة |
| `مُلئت N إطاراً` | فجواتٌ قصيرة سُدَّت | فوق ٢٠٪ من الإطارات → أعد التصوير |
| `صُحّح N انقلاباً` | مرّات تبادل اليدين | عددٌ كبيرٌ جداً يعني رصداً مضطرباً |

---

## لماذا تُرفض لقطة

تُرفض ولا تدخل المجموعة إن:

1. **رُصدت اليد في أقلّ من نصف الإطارات** — إضاءة، أو بُعد، أو ضبابية حركة.
2. **بقيت فجوةٌ أطول من السقف** — اليد خرجت من الإطار، أو حجبها الجسم.
3. **ردّها `take_quality`** — قِصَرٌ مفرط، أو حركةٌ ضئيلة لا تُميَّز عن
   الارتعاش.

التقرير يطبع اسم كلّ فيديو مرفوض وسببه في النهاية، فتعرف أيّها تعيد تصويره.

لقبولها مع تحفّظ: `npm run trainstill -- --keep-bad` — **لا تفعل** إلّا إن
كنت تفهم ما تُدخله.

---

## الخيارات

| الخيار | متى |
|---|---|
| `--dry-run` | افحص ولا تكتب ولا تدرّب |
| `--no-mirror` | الفيديو معكوسٌ أصلاً (انظر المصيدة أعلاه) |
| `--skip-extract` | درّب على `v6` الموجود بلا إعادة استخراج |
| `--keep-bad` | اقبل اللقطات المتحفَّظ عليها |
| `--strict-labels` | تخطَّ أيّ مجلّد ليس في ملفّ الربط |
| `--signer NAME` | اسم المُشير، يُحفظ في عمود الوصف |
| `--max-gap 0.25` | سقف الفجوة بالثواني |

للاستخراج وحده بلا تدريب:

```
npm run videos -- --videos data/DATA --labels-map data/DATA/labels_map.json --dry-run
```

---

## إعادة التشغيل لا تُكرّر الصفوف

`npm run trainstill` **يعيد بناء `v6` من الصفر** كلّ مرّة، ويُزيح السابق إلى
`.bak`.

السبب: `video_to_csv.py` يُلحق ولا يستبدل — وهو الصواب حين تضيف دفعة. لكنّ
أمراً واحداً يُشغَّل مرّتين يجب أن يُعطي النتيجة نفسها. بلا هذا تتضاعف كلّ
عيّنة، **فتقع النسخة في التدريب ونسختها في الاختبار، وترتفع الدقّة كذباً**.
الفيديوهات هي المصدر، والملفّ مشتقٌّ منها.

---

## إضافة كلمات جديدة لاحقاً

1. صوّر ١٠ لقطات للكلمة.
2. `data/DATA/<اسم الكلمة>/` وضعها فيه.
3. أضف السطر إلى `labels_map.json`.
4. `npm run trainstill` — يُعاد البناء كاملاً.

لا حاجة لحذف شيء ولا لترحيل.

---

## أخطاء شائعة

| ما تراه | السبب | الحلّ |
|---|---|---|
| `لا يوجد ملفّ الربط` | لم يُنشأ بعد | راجع القسم ١ |
| كلّ الفيديوهات مرفوضة، «اليد في ٠٪» | العدسة بعيدة، أو الإضاءة خلفية | قرّب وصوّر والضوء أمامك |
| الدقّة عالية والاستدلال الحيّ سيّئ | العكس المزدوج | `--no-mirror` |
| `ليس في ملفّ الربط، سيدخل باسمه` | مجلّدٌ جديد | أضفه للربط أو `--strict-labels` |
| `فيه ٠ صفّاً — لا شيء يُدرَّب عليه` | رُفض كلّ شيء | اقرأ أسباب الرفض أعلاه |

> ⚠️ **أوقف الخادم قبل التدريب.** ويندوز يقفل `sign_model.onnx` أثناء
> تشغيل الخادم، فتفشل الكتابة.

---

## الفرق بين `v5` و `v6`

| | `dynamic_gestures_v5.csv` | `dynamic_gestures_v6.csv` |
|---|---|---|
| المصدر | التقاطٌ حيّ (`npm run collect`) | فيديوهات (`npm run trainstill`) |
| الأعمدة | `label` + السمات | `label` + وصفٌ فيه `source` + السمات |

الملفّان مستقلّان. `npm run trainstill` يدرّب على `v6` وحده — لا يمسّ `v5`.

للتدريب على الاثنين معاً يلزم دمجهما بعمود `source` سليم. ابدأ بـ`v6`
وحده لترى جودة الفيديوهات أولاً: **خلطُ طريقتَي تسجيل يُدخل إزاحة مجال،
وقد يتعلّم المصنّف الكاميرا بدل الإشارة.** وعمود `source` هو ما يجعل هذا
قابلاً للقياس بدل أن يبقى تخميناً.

---

## كيف تقرأ نتيجة التدريب

القطر في `confusion_matrix.png` = الإجابات الصحيحة. الأخطاء **خارجه**.

والرقم الذي يهمّ هو التحقّق المتقاطع (`mean ± std`) لا الدقّة على مجموعة
الاختبار — لأنّه يختبر كلّ عيّنة لا ٢٠٪ منها.
