# تقرير المعمارية الشامل: مشروع ترجمان (TarjumanV1)

## 1. نظرة عامة على المشروع
مشروع "ترجمان" هو نظام هجين للتعرف على لغة الإشارة وترجمتها إلى نصوص وصوت، بالإضافة إلى احتوائه على مساعد ذكي مدمج. يعتمد النظام على معمارية "الخادم بدون واجهة مرئية" (Zero-Bandwidth Headless Camera)، حيث تتم جميع معالجات الفيديو، وتحليل الإيماءات، ونداءات الذكاء الاصطناعي على الخادم (Backend)، بينما يقتصر دور الواجهة الأمامية (التي يبدو أنها مبنية بتقنيات الويب/Next.js) على إرسال واستقبال بيانات خفيفة جداً بتنسيق JSON، مما يقلل بشكل كبير من استهلاك النطاق الترددي (Bandwidth).

## 2. مكونات النظام الأساسية (Backend)

### أ. خادم الويب سوكيت (`websocket_server.py`)
هو القلب النابض للنظام، مبني باستخدام مكتبات `asyncio` و `websockets`.
- **المهام الأساسية:** استقبال أوامر التشغيل والإيقاف من العميل، تشغيل حلقة الكاميرا (Camera Loop) في مسار تنفيذي منفصل (Thread Pool) لتجنب تجميد الخادم، معالجة الإطارات باستخدام MediaPipe لاستخراج الإحداثيات، استدعاء نموذج تعلم الآلة (Scikit-learn) للتنبؤ بالحرف، وإرسال النتائج بشكل لحظي للعميل.
- **تكاملات الذكاء الاصطناعي (AI):**
  - **تحويل النص إلى كلام (TTS):** يتم باستخدام أداة `edge-tts` لتوليد صوت باللغة العربية وإرساله كبيانات ثنائية (Binary) للعميل.
  - **تحويل الكلام إلى نص (STT):** يتم عبر واجهة Groq البرمجية السريعة (باستخدام نموذج Whisper-large-v3).
  - **المساعد الذكي (LLM):** يتم استدعاؤه عبر OpenRouter (باستخدام نموذج perplexity/sonar) للإجابة على استفسارات المستخدم باللغة العربية الفصحى مع التشكيل.

### ب. طبقة إدارة الكاميرا الذكية (`camera_manager.py`)
طبقة تجريد (Abstraction Layer) للتعامل مع الكاميرا بذكاء باختلاف بيئة التشغيل.
- تعمل باستخدام `cv2.VideoCapture` على أجهزة الحاسوب الشخصية واللابتوب للتطوير.
- تكتشف تلقائياً بيئة Raspberry Pi وتتحول لاستخدام `Picamera2` مع إعدادات تركيز بؤري يدوي (Manual Focus) وثبات في الإطارات (30 FPS) لضمان أداء مستقر في بيئة الإنتاج.

### ج. نظام جمع بيانات التدريب (`data_collector.py`)
أداة واجهة رسومية (تعمل محلياً) لتسجيل حركات لغة الإشارة الديناميكية.
- تسجل مقاطع حركية تعتمد على نافذة منزلقة (Sliding Window) تتكون من 30 إطار متتالي.
- تستخرج 300 نقطة (Landmarks) من MediaPipe (موزعة على الوجه، اليد اليسرى، واليد اليمنى) في كل إطار.
- تحفظ البيانات في ملف `dynamic_gestures.csv` بحجم عملاق يبلغ 9000 ميزة لكل صف (300 نقطة × 30 إطار).

### د. خط تدريب النموذج (`train_model.py`)
مسار متقدم (Pipeline) لتدريب نموذج `RandomForestClassifier` على البيانات الحركية الديناميكية.
- يقرأ ملف CSV ويقوم بمعالجة البيانات وتوحيدها (StandardScaler).
- يستخدم تقنية مبتكرة لتعزيز البيانات (Data Augmentation) عبر إضافة ضجيج جاوسي (Gaussian Jitter) لمحاكاة ارتجاف اليد أو الاختلافات البسيطة في الموقع.
- يقوم بتصدير النموذج المدرب كملف `.pkl` وخريطة التصنيفات كملف `labels.json`، بالإضافة إلى رسم وحفظ مصفوفة الارتباك (Confusion Matrix).

### هـ. الأداة البديلة لمعالجة الصور الثابتة (`main.py`)
سكربت مخصص لاستخراج الإحداثيات من صور ثابتة مجمعة مسبقاً (موجودة في مجلد `dataset`). يركز على اليد فقط ويقوم بعمل تسوية (Normalization) للإحداثيات بناءً على موقع المعصم لتوحيد الأحجام قبل الحفظ في ملف `dataset.csv`.

---

## 3. التحليل المعماري ونقاط الضعف الحرجة (Critical Vulnerabilities & Anti-Patterns)
بصفتي مهندس النظم ومعماري الأكواد، اكتشفت عدة عيوب جوهرية يجب التعامل معها بحزم:

1. **تسريب المفاتيح السرية (Security - OWASP):** 
   - ملف `websocket_server.py` يحتوي على مفاتيح API حقيقية ومكشوفة بالكامل لخدمتي OpenRouter و Groq. هذا خرق أمني خطير يجب إصلاحه فوراً بنقل هذه المفاتيح إلى متغيرات بيئة (Environment Variables) عبر ملف `.env`.
   
2. **اختناق الأداء واستهلاك الموارد (Performance Bottleneck):**
   - استخدام نموذج `RandomForest` من مكتبة `scikit-learn` لعمل استدلال (Inference) على مصفوفة بحجم 9000 ميزة في كل إطار يستهلك المعالج بشدة. يُنصح بشدة تحويل النموذج إلى تنسيق **ONNX** لتمكين التسريع العتادي، خصوصاً وأن النظام مستهدف للعمل على أجهزة محدودة الموارد مثل Raspberry Pi.

3. **مشكلة التزامن (Concurrency Anti-pattern):**
   - في الخادم غير المتزامن (`asyncio`)، يتم استخدام مكتبة `requests` المتزامنة للاتصال بواجهة Groq STT. على الرغم من تغليفها داخل `executor`، إلا أن هذا الأسلوب غير مفضل ويفضل استخدام مكتبة I/O غير متزامنة مثل `aiohttp` للحصول على أداء يتسع بشكل سليم (Scalable).

4. **الارتباط الوثيق بالعتاد وقابلية التوسع (Hardware Coupling):**
   - الخادم حالياً يدير كاميرا مادية واحدة (`SmartCamera`). إذا اتصل أكثر من عميل (Client) بالخادم وحاول كل منهم طلب فتح الكاميرا، سيفشل النظام أو يتوقف. هذه المعمارية تجعل النظام يعمل كـ "كشك محلي" (Kiosk System) وليس كخدمة شبكية حقيقية متعددة المستخدمين.
