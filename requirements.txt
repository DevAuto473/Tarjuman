# Tarjuman — Python dependencies
#
# NOTE: picamera2 is deliberately NOT listed here. It is Raspberry Pi only,
# imported lazily inside a try/except in camera_manager.py, and is installed
# via apt on the Pi (`sudo apt install -y python3-picamera2`), not via pip.
# Listing it would break `pip install -r requirements.txt` on Windows/macOS.

absl-py==2.4.0
aiohappyeyeballs==2.6.2
aiohttp==3.14.1
aiosignal==1.4.0
annotated-types==0.7.0
anyio==4.13.0
attrs==26.1.0
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
contourpy==1.3.3
cycler==0.12.1
distro==1.9.0
edge-tts==7.2.8
flatbuffers==25.12.19
fonttools==4.63.0
frozenlist==1.8.0
groq==1.4.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jax==0.10.1
jaxlib==0.10.1
jiter==0.15.0
joblib==1.5.3
kiwisolver==1.5.0
matplotlib==3.11.1
mediapipe==0.10.14
ml_dtypes==0.5.4
multidict==6.7.1
numpy==2.4.6
onnx==1.22.0
onnxruntime==1.28.0
openai==2.41.1
# OpenCV — install exactly ONE of these. They all provide the same `cv2`
# module and overwrite each other's files, so the last one installed silently
# wins and `pip list` shows a package that is not the one being imported.
#
# This pin used to read opencv-python-headless==4.9.0.80, which was wrong in
# two ways at once:
#   * headless builds ship NO GUI, yet data_collector and test_model are built
#     entirely around cv2.imshow;
#   * 4.9.0.80 predates NumPy 2 and was compiled against NumPy 1.x, so with
#     numpy==2.4.6 it dies on import with "_ARRAY_API not found".
# The dev laptop had opencv-contrib-python 5.0.0.93 installed on top by hand,
# which is why everything worked there while a fresh clone could not even
# import cv2. Recording what actually works, so the next machine matches.
opencv-contrib-python==5.0.0.93
opt_einsum==3.4.0
packaging==26.2
pandas==3.0.5
pillow==12.2.0
pip==26.0.1
propcache==0.5.2
protobuf==4.25.9
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pygrabber==0.2
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-dotenv==1.0.1
pytz==2026.2
requests==2.34.2
scikit-learn==1.4.2
scipy==1.17.1
seaborn==0.13.2
setuptools==80.10.2
six==1.17.0
# skl2onnx is deliberately NOT required. It crashed the interpreter with
# 0xC0000005 on Windows, blocking training entirely, and the graph it was
# producing (a Scaler plus a TreeEnsembleClassifier) is emitted directly by
# onnx_export.py using `onnx` alone - verified against scikit-learn on every
# training run. One less wheel to install on the Raspberry Pi, too.
sniffio==1.3.1
sounddevice==0.5.5
srt==3.5.3
tabulate==0.10.0
threadpoolctl==3.6.0
tqdm==4.68.2
typing_extensions==4.15.0
typing-inspection==0.4.2
tzdata==2026.2
urllib3==2.7.0
websockets==12.0
yarl==1.24.2