# أوامر ترجمان — المرجع الكامل

> كل الأوامر تُشغَّل من جذر المشروع:
> `C:\Users\HP\Desktop\pojects tree\TarjumanV1 (Copy)`

---

## الدورة اليومية — احفظ هذه الأربعة فقط

```
npm run collect      # ١. سجّل كلمات
npm run train        # ٢. درّب النموذج
npm run test         # ٣. جرّب مباشرةً بالكاميرا
npm run dev:browser  # ٤. شغّل التطبيق كاملاً
```

الباقي أدوات تُستدعى عند الحاجة فقط.

---

## ١. التسجيل والتدريب

### `npm run collect` — تسجيل الإشارات

يسألك بالترتيب: **الكاميرا** ← **الكلمة** ← **عدد العيّنات** ← **نمط التسجيل**.

أنماط التسجيل الثلاثة:

| النمط | متى تستعمله |
|---|---|
| **1. Dynamic** | إشارات متحرّكة — سلسلة تلقائية مع ثانية بين كل عيّنة |
| **2. Static** | أشكال يد ثابتة (أرقام، حروف) — بلا توقّف |
| **3. Manual** *(الافتراضي)* | عيّنة واحدة لكل ضغطة `r` — الأفضل حين تريد تغيير المسافة أو الزاوية بين العيّنات |

داخل النافذة: `r` = ابدأ التسجيل · `q` = خروج مبكر

**بعد كل كلمة** يظهر خياران: إنهاء، أو **تسجيل كلمة أخرى بدون إغلاق الكاميرا**. اختر ٢ لتسجيل عدة كلمات في جلسة واحدة — أسرع بكثير من إعادة التشغيل.

### `npm run train` — تدريب النموذج

يقرأ `data/dynamic_gestures_v4.csv` ويُخرج:

- `sign_model.onnx` ← الذي يستعمله الخادم فعلاً
- `sign_model.pkl` ← للفحص المحلّي فقط
- `data/labels.json` ← خريطة الأرقام إلى الكلمات
- `confusion_matrix.png` ← **اقرأ هذه، لا الدقّة**

> ⚠️ **أوقف الخادم قبل التدريب.** ويندوز يقفل `sign_model.onnx` أثناء تشغيل الخادم، فتفشل الكتابة.

**كيف تقرأ النتيجة:** القطر في مصفوفة الالتباس = الإجابات **الصحيحة**. الأخطاء تظهر **خارجه**. والرقم الذي يهمّ هو التحقّق المتقاطع (`mean ± std`) لا الدقّة على مجموعة الاختبار — لأنه يختبر كل عيّنة لا 20% منها.

للتخطّي عند المفردات الكبيرة: `TARJUMAN_SKIP_CV=1`

### `npm run test` — تجربة النموذج مباشرةً

نافذة كاميرا تعرض ما يتعرّف عليه النموذج لحظياً مع نسبة الثقة.

`q` = خروج · `r` = إعادة تعيين · `مسافة` = إيقاف مؤقّت

> اختر **نفس الكاميرا** التي سجّلت بها. عدسة مختلفة تزحزح كل النقاط، والنتيجة تبدو وكأن النموذج سيّئ.

**اقرأ سطر التفصيل أعلى النافذة:** `cam 42 hands 17 pose 3 draw 0 show 3 ms`.
إن كان `cam` أكبر من مجموع الباقي، فالكاميرا هي العنق لا الكود — والسبب شبه
الدائم هو **ضوء الغرفة**: الكاميرا تطيل التعريض في الإضاءة الخافتة فينخفض معدّلها
إلى النصف. الإضاءة الأقوى ترفع الـ fps **وتزيل ضبابية الحركة** عن اليد المتحرّكة.

---

## ٢. تشغيل التطبيق

| الأمر | ما يفعله |
|---|---|
| `npm run dev:browser` | الخادم + التطبيق في المتصفِّح — **لا يحتاج Rust** |
| `npm run dev:all` | الخادم + تطبيق سطح المكتب — **يحتاج Rust مثبّتاً** |
| `npm run dev:server` | الخادم وحده |
| `npm run dev:web` | الواجهة وحدها — تنتظر خادماً يعمل مسبقاً |
| `npm run dev:app` | تطبيق سطح المكتب وحده — ينتظر خادماً يعمل مسبقاً |
| `npm run build` | بناء نسخة الإصدار من تطبيق سطح المكتب |

> إن ظهر `cargo metadata: program not found` فأنت لم تثبّت Rust. استعمل `dev:browser`، أو ثبّت Rust من [rustup.rs](https://rustup.rs) وأعد فتح PowerShell.

الخادم يسألك عن الكاميرا في الطرفية عند الإقلاع. لتخطّي السؤال، ضع في `.env`:

```
CAMERA_SOURCE=index:2     # على اللابتوب
CAMERA_SOURCE=picamera    # على الراسبيري باي (كاميرا CSI)
```

---

## توزيع الأجهزة — لابتوب + راسبيري باي

| الجهاز | الدور | الكاميرا |
|---|---|---|
| اللابتوب | التدريب (`npm run train`) | webcam — الخيارات ١-٤ |
| راسبيري باي ٥ | التسجيل والاستنتاج | Camera Module 3 عبر CSI — **الخيار ٥** |

الخيار الخامس يظهر في قائمة `Select camera` تلقائياً على الراسبيري فقط، ويصبح
الخيار الافتراضي هناك. على ويندوز لا يظهر إطلاقاً، ولا يُستورَد `picamera2` أصلاً.

تثبيت المكتبة على الراسبيري:

```bash
sudo apt install -y python3-picamera2
```

> **بـ apt لا pip.** الحزمة مبنيّة على مكدّس `libcamera` الخاص بالنظام، ونسخة pip لن تعمل.

> ⚠️ **حافظ على تطابق الكاميرا بين التسجيل والاستنتاج.** إن سجّلت عيّنات على
> اللابتوب وأخرى على الراسبيري، فأنت تخلط عدستين بمجالَي رؤية مختلفين في مجموعة
> واحدة — وهذا يزحزح كل النقاط ويبدو لاحقاً وكأن النموذج سيّئ. سجّل كل شيء على
> الجهاز الذي سيعمل عليه المنتج النهائي.

---

## ٣. الروبوت ثلاثي الأبعاد

| الأمر | ما يفعله |
|---|---|
| `npm run export3d` | يحوّل تسجيلاتك إلى حركات للروبوت (`trained_signs.json`) |
| `npm run mirror` | نافذتان: أنت والهيكل المشتقّ من الرياضيات — لتشخيص الحركة بدون متصفّح |

شغّل `export3d` بعد كل تدريب، وإلا بقي الروبوت يؤدّي الكلمات القديمة.

---

## ٤. أدوات التشخيص — عند حدوث مشكلة

| الأمر | متى |
|---|---|
| `npm run bench` | **الـ fps منخفض** — يقيس أين تذهب كل مللي ثانية، ويمسح أوضاع الكاميرا |
| `npm run doctor` | **بايثون ينهار بلا رسالة** — يحدّد الحزمة المسبِّبة |
| `npm run fix` | **خطأ في نسخ المكتبات** — يُعيد النسخ المثبّتة ويتحقّق أن MediaPipe يعمل فعلاً |
| `npm run cameras` | قائمة الكاميرات بأسمائها الحقيقية |
| `npm run cameras -- --preview` | معاينة حيّة لمعرفة أي فهرس هو أي كاميرا |

**قراءة سريعة لمُخرَج `bench`:** إن كان «sensor delivers» أقل بكثير من سقف MediaPipe، فالكاميرا هي العنق ولا فائدة من أي تحسين برمجي.

---

## ٥. أدوات المفردات

| الأمر | ما يفعله |
|---|---|
| `npm run sheet` | يحدّث `data/learn.csv` — كم عيّنة سجّلت لكل كلمة وكم يتبقّى |
| `npm run check` | يفحص المفردات: هل توجد إشارتان متطابقتان في كل المعاملات؟ |
| `npm run migrate` | تحويل مجموعة بيانات قديمة إلى التنسيق الحالي (نادر) |

---

## متغيّرات `.env`

```bash
CAMERA_SOURCE=index:2     # تخطّي سؤال الكاميرا
CAMERA_WIDTH=640          # شغّل npm run bench لمعرفة أفضل وضع
CAMERA_HEIGHT=480
CAMERA_FPS=60
CAMERA_EXPOSURE=-6        # تثبيت التعريض يدوياً (اختياري) — log2(ثانية) على ويندوز
CAMERA_GAIN=150           # تعويض الإضاءة الخافتة بدون إطالة التعريض (اختياري، 0-255)
TARJUMAN_SKIP_CV=1        # تخطّي التحقّق المتقاطع (أسرع)
TARJUMAN_NO_PROMPT=1      # تخطّي كل أسئلة الكاميرا
```

> `CAMERA_EXPOSURE`/`CAMERA_GAIN` غير مطلوبين عادةً — الكاميرا تبحث تلقائياً عن
> أفضل تعريض عند الإقلاع (انظر أعلاه). ثبّتهما فقط بعد إضافة إضاءة فعلية
> وإيجاد قيم تعرف أنها تعمل جيداً، لتخطّي البحث التلقائي والحصول على نفس
> النتيجة في كل تشغيل. رفع `CAMERA_GAIN` أرخص من تطويل التعريض: يكلّف ضوضاء
> أكثر في الصورة، لا فريمات أقل.

المفاتيح السرّية في `.env` فقط. **لا تضعها في `.env.example`** — هذا الملف يُرفع إلى git.

---

## أخطاء شائعة وحلولها

| العَرَض | السبب |
|---|---|
| `exited with code 3221225477` | انهيار مكتبة أصلية ← `npm run doctor` |
| `Cannot write sign_model.onnx` | الخادم شغّال ويقفل الملف ← أوقفه |
| التدريب لا يُخرج شيئاً | خطأ ترميز أو ملف مقفل ← الرسالة صارت صريحة الآن |
| الروبوت يؤدّي كلمات قديمة | نسيت `npm run export3d` |
| النموذج يخطئ فجأة | جرّبت بكاميرا غير التي سجّلت بها |
| `cargo not found` | Rust غير مثبّت ← `dev:browser` |
| `'FieldDescriptor' object has no attribute 'label'` | نسخة protobuf أحدث من اللازم ← `npm run fix` |
| `MessageFactory has no attribute GetPrototype` | نفس السبب أعلاه ← `npm run fix` |
| الـ fps انخفض فجأة والغرفة مظلمة | الكاميرا أطالت التعريض فخفّضت معدّلها — **أضِف إضاءة** |

---

## تشغيل المشروع على جهاز جديد — أمران فقط

```bash
git clone https://github.com/DevAuto473/Tarjuman.git
cd Tarjuman
npm run setup
```

`npm run setup` يتولّى كل شيء: يتحقّق من نسخة بايثون، ينشئ البيئة الافتراضية
(مع `--system-site-packages` تلقائياً على الراسبيري)، يثبّت الحزم، يفكّ تشابك
حِزم OpenCV إن وُجد، ثم **يتحقّق أن كل شيء يعمل فعلاً** — يبني رسم MediaPipe
حقيقياً ويفكّ ترميز إطار بـ OpenCV، لا يكتفي بنجاح الاستيراد.

آمن لإعادة التشغيل، **ولا يستبدل `.env` أبداً** إن كان موجوداً.

يحتاج Node.js وPython 3.11 مثبَّتين مسبقاً — وإن نقص أحدهما يخبرك بأمر التثبيت الدقيق.

> **لماذا وُجد هذا الأمر:** إعداد الجهاز الثاني استغرق ساعات وثلاثة أعطال، ولا
> واحد منها ذكر سببه الحقيقي: بايثون ٣.٨ ظهر كأنه "حزمة مفقودة"، وOpenCV قديمة
> انهارت مع NumPy 2، وprotobuf حديثة كسرت MediaPipe **بعد** نجاح الاستيراد.
> `npm run setup` يكشف الثلاثة في ثوانٍ.

**ما ينتقل عبر git:** الكود · `data/dynamic_gestures_v4.csv` · `data/labels.json` · `sign_model.onnx`

فتستطيع التدريب والتشغيل مباشرةً بلا نقل يدوي.

**ما لا ينتقل — ولماذا:**

| الملف | السبب | ماذا تفعل |
|---|---|---|
| `.env` | **يحوي مفاتيح API** | انسخه يدوياً (فلاش/مدير كلمات مرور) — **لا ترفعه أبداً** |
| `venv/` · `node_modules/` | خاصّة بنظام التشغيل والمعمارية | `pip install` و `npm install` |
| `sign_model.pkl` | pickle هشّ بين النسخ، ويُولَّد من التدريب | `npm run train` |
| النسخ الاحتياطية `*.before-*.csv` | كبيرة ولا يقرأها أي كود | تبقى محلّية |

> 🔑 **قاعدة لا تُكسَر:** المفاتيح تُنقَل بيدك، لا عبر git. ملف `.env` مرفوع مرة واحدة على مستودع عام يعني مفاتيح محروقة — وحذفه لاحقاً **لا يمحوه** من سجلّ git.

---

## بنية المشروع

```
data/                 المجموعات + labels.json + learn.csv
src/tarjuman_core/    المكتبة المشتركة (لا تُشغَّل مباشرةً)
scripts/              أدوات مساعدة + مشغّل بايثون
tarjuman/             تطبيق React / Tauri
*.py (الجذر)          نقاط التشغيل
```

---

## المسار الكامل من الصفر

```
npm run check        # تأكّد أن المفردات بلا تصادم
npm run collect      # سجّل — ٣٠ عيّنة لكل كلمة، وحافظ على التوازن
npm run sheet        # راجع ما تبقّى
npm run train        # درّب (أوقف الخادم أولاً)
npm run test         # جرّب بنفس الكاميرا
npm run export3d     # حدّث حركات الروبوت
npm run dev:browser  # شغّل التطبيق
```

**القاعدتان اللتان تنقذانك من أكثر المشاكل:** سجّل عدداً **متساوياً** لكل كلمة، واستعمل **نفس الكاميرا** في التسجيل والتشغيل.
